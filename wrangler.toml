name = "nextjs-app"
compatibility_date = "2025-01-01"

# Cloudflare Pages configuration
pages_build_output_dir = ".next"

# Environment variables
[vars]
NODE_ENV = "production"

# Custom domains (replace with your domain)
# [[routes]]
# pattern = "yourdomain.com/*"
# zone_name = "yourdomain.com"
